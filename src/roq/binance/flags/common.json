{
  "name": "Common",
  "type": "flags",
  "values": [
    {
      "name": "request_limit_interval",
      "type": "std::chrono::nanoseconds",
      "validator": "roq::flags::validators::TimePeriod",
      "default": "5s",
      "description": "Request limit monitor interval"
    },
    {
      "name": "request_limit",
      "type": "uint32_t",
      "default": "8",
      "description": "Request limit per interval"
    },
    {
      "name": "encode_buffer_size",
      "type": "uint32_t",
      "required": true,
      "default": 1048576,
      "description": "Encode buffer size"
    },
    {
      "name": "decode_buffer_size",
      "type": "uint32_t",
      "required": true,
      "default": 10485760,
      "description": "Decode buffer size"
    },
    {
      "name": "mbp_allow_price_inversion",
      "type": "bool",
      "default": false,
      "description": "Allow price inversion?"
    },
    {
      "name": "mbp_max_depth",
      "type": "uint16_t",
      "description": "Maximum depth levels (count)"
    },
    {
      "name": "mbp_sequencer_timeout",
      "type": "std::chrono::nanoseconds",
      "validator": "roq::flags::validators::TimePeriod",
      "default": "120s",
      "description": "MbP sequencer timeout"
    },
    {
      "name": "cancel_replace_stop_on_failure",
      "type": "bool",
      "default": false,
      "description": "If the cancel request fails, the replacement will not be attempted"
    },
    {
      "name": "download_symbols",
      "type": "std::vector<std::string>",
      "description": "List of symbols to download"
    },
    {
      "name": "download_trades_lookback",
      "type": "std::chrono::nanoseconds",
      "validator": "roq::flags::validators::TimePeriod",
      "default": "300s",
      "description": "Download trades? (lookback period)"
    },
    {
      "name": "download_trades_lookback_on_restart",
      "type": "std::chrono::nanoseconds",
      "validator": "roq::flags::validators::TimePeriod",
      "default": "{}",
      "description": "Download trades? (lookback period)"
    },
    {
      "name": "download_trades_limit",
      "type": "uint32_t",
      "default": 1000,
      "description": "Exchange limitation"
    }
  ]
}
