{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "binance",
      "description": "exchange identifier (string)"
    },
    {
      "name": "ws_api",
      "type": "bool",
      "default": false,
      "description": "use the websocket api?"
    }
  ]
}
